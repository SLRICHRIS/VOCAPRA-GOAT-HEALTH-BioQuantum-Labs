# Streamlit web UI
streamlit

# TensorFlow CPU build (sufficient for inference)
tensorflow-cpu==2.15.0

# Audio + signal processing
librosa==0.10.1
soundfile==0.12.1

# Numeric stack (pinned so TF + librosa are happy)
numpy<2.0
scipy

# Plotting
matplotlib
